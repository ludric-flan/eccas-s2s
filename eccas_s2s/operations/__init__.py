"""
Operational steps of the OSF chain.

Each module exposes a ``run(...)`` function used by the operational notebooks
(``notebooks/operationnels/``) and a ``main(argv)`` used by the thin command-line
wrappers in ``scripts/``. Both paths therefore execute exactly the same code.
"""
